<div id="flights-index">
  <div class="container">
    <h1>FLIGHTS</h1>

    <div class="flightcards">

      <div class="d-flex justify-content-between w-100 mb-3">
        <% if @flight.photo.attached? %>
          <%= cl_image_tag @flight.photo.key, height: 300, width: 400, crop: :fill, class:"flightpic"%>
        <% end %>
      </div>

  </div>
</div>
